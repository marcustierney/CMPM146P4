The heuristics we chose to add limit repeated tasks. The call stack is stopped from having more than 2 identical tasks using an if statement. This prevents infinite recursion of the crafting dependencies. The methods ordering sorts the methods by their time cost with the lowest first. This allows for test cases a, b, and c to pass successfully. D, e, and f fail due to resource and time constraits within the test case criterias.  